{
  "name": "remotevoice-cf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RemoteVoice 统一 Worker（PWA 静态资产 + Durable Object WebSocket 中继）。部署配置在仓库根 wrangler.jsonc。",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.6.2"
  }
}
